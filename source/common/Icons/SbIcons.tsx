import { faTrash } from '@fortawesome/free-solid-svg-icons';
import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';

export const sbDeleteIcon = faTrash;

export const sbDelete = <FontAwesomeIcon icon={sbDeleteIcon} />;
